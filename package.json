{
  "name": "aquamind",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20.19.0"
  },
  "workspaces": [
    "aquamind/client"
  ],
  "scripts": {
    "dev": "npm --workspace aquamind/client run dev",
    "build": "npm --workspace aquamind/client run build && node scripts/sync-dist.mjs",
    "preview": "npm --workspace aquamind/client run preview",
    "lint": "npm --workspace aquamind/client run lint"
  }
}
