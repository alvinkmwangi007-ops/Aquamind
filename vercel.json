{
  "version": 2,
  "buildCommand": "npm --workspace aquamind/client run build",
  "installCommand": "npm install",
  "outputDirectory": "aquamind/client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}